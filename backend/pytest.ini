[pytest]
# Ensure the backend package root (./backend) is always on sys.path.
# Without this, running `pytest` as a console script may fail to import `app`,
# while `python -m pytest` happens to work depending on environment.
pythonpath = .
testpaths = tests

